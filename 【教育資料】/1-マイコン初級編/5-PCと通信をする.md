# 5 - PCと通信をする

Arduinoを使っていると、「PCとデータをやり取りしたい！」と思うことがあります。
そんなときに使うのが Serial 通信です。

Serial通信を使うと

- PC → マイコンへデータを送る
- マイコン → PCへデータを送る

ということができるようになります。

最初は少し難しそうに見えますが、実際にやることはかなりシンプルです。

## 必要なもの
PC

最初に、setup() の中で通信速度を設定します。

```cpp
void setup() {
  Serial.begin(9600);
}
```
この 9600 は通信速度です。
PC側とArduino側で同じ値にしておく必要があります。

これで「通信を始める準備」ができました。
`Serial`を使う

